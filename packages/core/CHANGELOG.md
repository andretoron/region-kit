# region-kit

## 0.1.0

### Minor Changes

- 88f26cb: Add the Region Dataset Contract, schema compatibility validation, structured dataset validation errors, and the `validateRegionDataset` API.
- cdc3e0d: Add the public RegionKit facade with JSON loading, lookup, search, filtering, hierarchy traversal, deterministic sorting, pagination, lifecycle handling, and operational errors.
- cdc3e0d: Add the public `RegionStore` contract, query result types, and validated indexed `MemoryRegionStore` implementation.

### Patch Changes

- cdc3e0d: Add MVP usage documentation, dataset and hierarchy concepts, error handling, custom store guidance, known limitations, and migration policy.
- cdc3e0d: Add npm package metadata, changelog distribution, discovery keywords, and trusted publishing configuration.
