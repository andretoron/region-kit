---
"region-kit": minor
---

Add the public RegionKit facade with JSON loading, lookup, search, filtering, hierarchy traversal, deterministic sorting, pagination, lifecycle handling, and operational errors.
